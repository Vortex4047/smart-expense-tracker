# smart-expense-tracker
A fully Python-based command-line application that automates expense tracking by extracting text from receipt images using OCR. It processes receipts, parses date and amount, categorizes them, and stores the data in a SQLite database. Includes summary analytics for monthly spending insights.

Key Features:

📷 Reads receipts using OpenCV and Tesseract OCR

🧠 Extracts and parses expense details (date, amount, vendor)

📊 Generates monthly expense summaries with Pandas

💾 Local database storage with SQLite

🖥️ Lightweight CLI interface (100% Python, no frontend needed)

Tech Stack:
Python, OpenCV, Pytesseract, Pandas, SQLite

Why it’s impactful:
Demonstrates core skills in image processing, data parsing, file handling, and database integration — ideal for data science, automation, and fintech-oriented roles.
